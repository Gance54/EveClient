<Window x:Class="EveIndyCalc.RegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register" Height="250" Width="300"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="EmailBox"
             VerticalAlignment="Center"
             TextChanged="EmailBox_TextChanged" />
        <TextBlock x:Name="EmailPlaceholder"
               Text="Email"
               Foreground="Gray"
               Margin="5,0,0,0"
               VerticalAlignment="Center"
               IsHitTestVisible="False"
               Visibility="Visible" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,5" />
        <PasswordBox x:Name="ConfirmBox" Grid.Row="2" Margin="0,5" />

        <Button Grid.Row="3" Content="Register" Margin="0,10" Click="Register_Click"/>
        <TextBlock x:Name="StatusText" Grid.Row="4" Foreground="Red" Margin="0,5"/>
    </Grid>
</Window>